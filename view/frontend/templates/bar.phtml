<?php
/** @var $block \LeSite\CustomBar\Block\Bar */
/** @var $escaper \Magento\Framework\Escaper */

if ($block->isActive()) {
    echo $escaper->escapeHtml(
        __('Current customer group: %1', $block->getCurrentCustomerGroup()->getCode())
    );
}
